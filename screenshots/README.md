# Screenshot targets

Run the app with a reachable Base RPC and capture these routes at 1440×1000:

- `discover.png`: `/`
- `token.png`: `/token/[a token emitted by Factory]`
- `launch.png`: `/launch`

This directory is intentionally kept in the source package. Live token content depends on current Factory logs.
