import {ADDRESSES} from '@/lib/constants'; export default function Page(){return <article className="mx-auto max-w-3xl px-4 py-14"><p className="text-sm font-bold uppercase tracking-[.2em] text-cyan">The short scroll</p><h1 className="mt-3 font-display text-5xl font-black">Markets should begin with clarity.</h1><p className="mt-7 text-lg leading-8 text-muted">Liqpad launches fixed-supply B20 tokens into a VVV-only Uniswap v4 market on Base. The position is locked from inception, the opening frame is protocol-defined, and exact-out swaps are intentionally disabled.</p><div className="paper my-9 rounded-3xl p-7"><h2 className="font-display text-2xl font-bold">The invariant</h2><p className="mt-3 leading-7">Every launch has 1 billion tokens, pairs with VVV, begins at quoted frame 115200, and uses a locked single-sided position. The 1% hook fee burns the B20 side and routes VVV 70% to the creator and 30% to Liqpad through DiemEngine.</p></div><h2 className="font-display text-2xl font-bold">What trust remains</h2><p className="mt-3 leading-7 text-muted">Protocol DIEM is governed separately from creator accruals. The Safe owner can unwind protocol DIEM; this does not grant access to creator funds held for claims.</p><h2 className="mt-10 font-display text-2xl font-bold">Live contracts</h2><div className="mt-4 space-y-2 font-mono text-xs text-muted">{Object.entries(ADDRESSES).map(([k,v])=><div className="card flex flex-col gap-1 p-3 sm:flex-row sm:justify-between" key={k}><span>{k}</span><a className="text-cyan" href={`https://basescan.org/address/${v}`} target="_blank">{v}</a></div>)}</div></article>}
