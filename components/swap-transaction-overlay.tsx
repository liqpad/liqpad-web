'use client';
type Props={kind:'approval'|'swap';status:'wallet'|'confirming'|'success';hash?:string};
export function SwapTransactionOverlay({kind,status,hash}:Props){const title=status==='wallet'?`Confirm ${kind} in wallet`:status==='confirming'?`${kind==='approval'?'Approval':'Swap'} confirming`:`${kind==='approval'?'Approval':'Swap'} complete`;return <div className="fixed inset-0 z-[100] grid place-items-center bg-[#090812]/90 p-4 backdrop-blur-md" role="dialog" aria-modal="true" aria-live="polite"><div className="card w-full max-w-sm p-7 text-center shadow-neon"><div className="relative mx-auto grid h-20 w-20 place-items-center"><div className={`absolute inset-0 rounded-full border-2 ${status==='success'?'border-cyan/60':'animate-spin border-cyan/20 border-t-cyan'}`}/><div className={`grid h-14 w-14 place-items-center rounded-full text-2xl ${status==='success'?'bg-cyan/15 text-cyan':'animate-pulse bg-magenta/15 text-magenta'}`}>{status==='success'?'✓':'◆'}</div></div><h2 className="mt-6 font-display text-2xl font-black capitalize">{title}</h2><p className="mt-3 text-muted">{status==='wallet'?'Review the request before signing.':status==='confirming'?'Waiting for confirmation on Base.':'Balances and quote are being refreshed.'}</p>{hash&&<a href={`https://basescan.org/tx/${hash}`} target="_blank" rel="noreferrer" className="mt-5 block text-sm text-cyan">View on BaseScan ↗</a>}</div></div>}
