'use client'; import Link from 'next/link'; import {useMemo,useState} from 'react'; import type {Launch} from '@/lib/data'; import {short} from '@/lib/data'; import {TokenImage} from '@/components/token-image';
function Empty(){return <div className="card col-span-full py-16 text-center"><svg className="mx-auto mb-5 h-28 w-28 text-magenta/70" viewBox="0 0 120 120" fill="none"><path d="M18 87c17-7 22-32 42-43 16-9 31-4 42 8" stroke="currentColor" strokeWidth="4"/><circle cx="69" cy="35" r="13" stroke="#40e8ff" strokeWidth="3"/><path d="m79 25 13-13M83 20l9 9" stroke="#40e8ff" strokeWidth="3"/><path d="M20 92c29-7 51 2 82-9" stroke="currentColor" strokeWidth="3"/></svg><h3 className="font-display text-xl font-bold">The lanterns are waiting</h3><p className="mt-2 text-muted">No launches were found in the current on-chain window.</p><Link href="/launch" className="btn btn-primary mt-6 inline-flex items-center">Launch the first token</Link></div>}
export function Discover({launches,vvvBalance,pendingProtocolFees}:{launches:Launch[];vvvBalance:string;pendingProtocolFees:string}){const [mode,setMode]=useState<'grid'|'table'>('grid');const [q,setQ]=useState('');const [sort,setSort]=useState('new');const items=useMemo(()=>launches.filter(x=>`${x.name} ${x.symbol} ${x.token}`.toLowerCase().includes(q.toLowerCase())),[launches,q]);return <div><section className="relative overflow-hidden px-4 pb-14 pt-16"><div className="mx-auto max-w-7xl"><p className="mb-3 text-sm font-bold uppercase tracking-[.24em] text-cyan">B20 × VVV · Base</p><h1 className="max-w-3xl font-display text-5xl font-black leading-[.95] md:text-7xl">Launch into the<br/><span className="text-magenta">liquid night.</span></h1><p className="mt-6 max-w-xl text-lg text-muted">One launch. One locked v4 position. A market denominated entirely in VVV.</p><Link href="/launch" className="btn btn-primary mt-8 inline-flex">Create a B20 <span className="ml-2">→</span></Link></div></section><section className="border-y border-white/10 bg-white/[.025]"><div className="mx-auto grid max-w-7xl grid-cols-2 px-4 py-5 md:grid-cols-3"><Stat label="Total launches" value={String(launches.length)}/><Stat label="VVV in engine" value={vvvBalance} detail={pendingProtocolFees}/><Stat label="Opening frame" value="~10K VVV FDV" extra="hidden md:block"/></div></section><section className="mx-auto max-w-7xl px-4 py-12"><div className="mb-6 flex flex-col gap-3 md:flex-row"><input aria-label="Search launches" value={q} onChange={e=>setQ(e.target.value)} className="input flex-1" placeholder="Search name, symbol, or address"/><select className="input md:w-44" value={sort} onChange={e=>setSort(e.target.value)} aria-label="Sort"><option value="new">Newest</option><option value="volume">Volume</option><option value="mc">Market cap</option><option value="change">% change</option><option value="traders">Traders</option></select><div className="grid grid-cols-2 gap-1 rounded-xl border border-white/10 p-1"><button onClick={()=>setMode('grid')} className={`btn px-4 ${mode==='grid'?'bg-white/10 text-cyan':''}`} aria-label="Card grid">▦</button><button onClick={()=>setMode('table')} className={`btn px-4 ${mode==='table'?'bg-white/10 text-cyan':''}`} aria-label="Table">☷</button></div></div>{!items.length?<Empty/>:mode==='grid'?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(x=><TokenCard key={x.token} x={x}/>)}</div>:<div className="overflow-x-auto card"><table className="w-full min-w-[900px] text-left text-sm"><thead className="text-muted"><tr>{['Token','Price','MC','24h vol','24h','Traders','Tx','Age','LP'].map(h=><th className="px-4 py-4" key={h}>{h}</th>)}</tr></thead><tbody>{items.map(x=><tr className="border-t border-white/10" key={x.token}><td className="px-4 py-4"><Link href={`/token/${x.token}`}><b>{x.name||'B20 Token'}</b> <span className="text-muted">{x.symbol||short(x.token)}</span></Link></td><td colSpan={7} className="px-4 text-muted">On-chain pool data indexing…</td><td className="px-4 text-cyan">Locked</td></tr>)}</tbody></table></div>}</section></div>}
function Stat({label,value,detail,extra=''}:{label:string;value:string;detail?:string;extra?:string}){return <div className={`min-w-0 border-l border-white/10 px-4 ${extra}`}><div className="text-xs uppercase tracking-widest text-muted">{label}</div><div className="mt-1 truncate font-display text-xl font-bold" title={value}>{value}</div>{detail&&<div className="mt-1 truncate text-xs text-cyan" title={detail}>{detail}</div>}</div>}
function TokenCard({x}:{x:Launch}){return <Link href={`/token/${x.token}`} className="card group p-5 transition hover:-translate-y-1 hover:border-magenta/50"><div className="flex items-center gap-3"><div className="grid h-14 w-14 place-items-center overflow-hidden rounded-2xl bg-gradient-to-br from-magenta/30 to-cyan/20 text-xl font-black"><TokenImage src={x.image} alt={`${x.name} logo`} width={56} height={56} fallback={x.symbol||'?'}/></div><div><h3 className="font-display text-lg font-bold">{x.name||'B20 Token'}</h3><p className="text-sm text-muted">{x.symbol||short(x.token)} · VVV</p></div><span className="ml-auto rounded-full border border-cyan/30 px-2 py-1 text-xs text-cyan">◆ Locked</span></div><div className="mt-7 grid grid-cols-2 gap-4"><div><p className="text-xs text-muted">Price</p><p className="mt-1 font-semibold">Indexing…</p></div><div><p className="text-xs text-muted">Market cap</p><p className="mt-1 font-semibold">Indexing…</p></div></div><div className="mt-5 truncate border-t border-white/10 pt-4 font-mono text-xs text-muted">{x.token}</div></Link>}
